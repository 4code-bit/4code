{
  "name": "@fourcode/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "canvas-server (HTTP+WS en 127.0.0.1) y mcp-server (stdio). Procesos SEPARADOS: el stdio del MCP no admite ni un byte que no sea protocolo.",
  "scripts": {
    "canvas": "node src/canvas-server.ts",
    "mcp": "node src/mcp-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "ws": "^8.18.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/ws": "^8.18.1"
  }
}
